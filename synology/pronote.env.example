# Full address ending in eleve.html or parent.html
PRONOTE_URL=https://your-school.index-education.net/pronote/parent.html
PRONOTE_USERNAME=your-username
PRONOTE_PASSWORD=your-password
PRONOTE_AUTH_MODE=password

# auto detects parent/student from the URL. If the parent account contains
# several children, enter the exact displayed name of the desired child.
PRONOTE_ACCOUNT_TYPE=auto
PRONOTE_CHILD_NAME=

# Leave blank for a direct Pronote login. ENT users may need the corresponding
# PronotePy ENT function name.
PRONOTE_ENT=
PRONOTE_ACCOUNT_PIN=
PRONOTE_CREDENTIALS_PATH=/data/credentials.json

TZ=Europe/Paris
DAYS_PAST=7
DAYS_AHEAD=21
REFRESH_INTERVAL=900
OUTPUT_PATH=/data/homework.json

HTTP_HOST=0.0.0.0
HTTP_PORT=8080
API_KEY=replace-with-output-of-openssl-rand-hex-24

# Optional Mosquitto integration. Use the NAS LAN address, not 127.0.0.1,
# because localhost inside this container means the container itself.
MQTT_HOST=
MQTT_PORT=1883
MQTT_USERNAME=
MQTT_PASSWORD=
MQTT_TOPIC=pronote/homework
MQTT_QOS=1
MQTT_TLS=false
